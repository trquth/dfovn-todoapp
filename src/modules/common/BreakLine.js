import React from 'react';
import { View } from 'react-native';
import { Colors } from '../../themes';

export const BreakLine = () => {
  return <View style={{ height: 1, backgroundColor: Colors.grayLight }} />
}